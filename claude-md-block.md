# boink
- **boink** (`~/.claude/skills/boink/SKILL.md`) — passive anti-stall watchdog + session
  garbage collector. ALWAYS ACTIVE; the user never has to ask for it.
The moment you create ANY semantic monitor — Monitor tool, background Bash, poll loop,
CI/build/deploy watch, or any turn ending in "I'll check back when…" — invoke the Skill
tool with `skill: "boink"` and follow it. Do not wait to be asked.
